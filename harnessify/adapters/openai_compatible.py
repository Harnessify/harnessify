from __future__ import annotations


def openai_compatible_status() -> dict[str, str]:
    return {
        "available": "false",
        "message": "OpenAI-compatible adapter is not implemented in Milestone 1.",
    }
