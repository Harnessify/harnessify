from __future__ import annotations


def shell_agent_status() -> dict[str, str]:
    return {
        "available": "false",
        "message": "Shell adapter is a placeholder for future runtime wrapping.",
    }
