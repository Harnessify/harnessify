from __future__ import annotations


def openclaw_status() -> dict[str, str]:
    return {
        "available": "false",
        "message": "OpenClaw adapter is reserved for future runtime integration.",
    }
